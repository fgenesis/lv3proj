
function climit(cur, lower, upper)
    if(cur < lower)
        cur = upper
    elif(cur > upper)
        cur = lower
    end
    
    return cur
end

function arraysum(y): return reduce( {a,b => a+b}, y, 0 )

export
