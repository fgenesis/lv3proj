
// do some cleanup work before a package's game script is loaded
function InitGamePackage()
    CloseCurrentMenu()
    
end

function ShutdownGamePackage()
    Engine.Reset()
    
end

function TryInitGameScript(file)
    if not ScriptLoader.LoadFile(file)
        ShutdownGamePackage()
        return
    end
end

export
