function InitDefaultScreen()
    Screen.SetMode(1000, 600, false, true)
    Screen.SetBGColor(0,0,0)
    Screen.FrameLimit(60, 70)
    > "Default screen layout created"
end

// this is broadcasted by init.fal when script initialization is complete
subscribe("INIT_DONE", InitDefaultScreen)

//export
