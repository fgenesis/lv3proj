//load Player_PlayerEx

class Olaf from PlayerEx

    gfx = "sprites/olaf1.anim"

    function OnUpdate(ms)
        //> ms, " - Olaf Updated!"
        multi = ms * 0.001
        mv = (64 * multi)
        //> "move by:", mv
        self.x += mv
    end

end

export Olaf
